from schema_builder.builder_table_list import *
import pytest
import pytest_cov


@pytest.mark.skip(reason="Test needs to be created.")
def test_parse_formatted_table():
    # The data format for this test needs to be stubbed
    # In the carelogic_service run parse_table_schema() to get the needed data

    # clean_data = parse_formatted_table(table)
    pass

